version: "2"
# authtoken is read from NGROK_AUTH_TOKEN environment variable
# Set it with: export NGROK_AUTH_TOKEN=your_token_here

tunnels:
  chat-vault-part1:
    proto: http
    addr: 8000
  chat-vault-part2:
    proto: http
    addr: 8001
